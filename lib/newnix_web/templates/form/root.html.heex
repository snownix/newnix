<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="csrf-token" content={csrf_token_value()}>
    <%= live_title_tag assigns[:page_title] || "Newnix", suffix: " · Newnix" %>
    <meta name="description" content={assigns[:page_title] || "Newnix"}>
    <meta name="og:title" content={assigns[:page_description] || "Newnix"}>
    <meta name="og:description" content={assigns[:page_description] || "Newnix"}>
    <script defer phx-track-static type="text/javascript" src={Routes.static_path(@conn, "/assets/app.js")}></script>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">

    <style>
      body {
        font-family: 'Inter';
      }
      .newnix_form input,
      .newnix_form button {
        border: solid 1px rgba(25,25,25,0.4);
      }

      .newnix_form {
        margin: auto;
        display: block;
      }

      .newnix_form > :not([hidden]) ~ :not([hidden]) {
        --tw-space-y-reverse: 0;
        margin-top: calc(2rem * calc(1 - var(--tw-space-y-reverse)));
        margin-bottom: calc(2rem * var(--tw-space-y-reverse));
      }

      .newnix_form {
        --tw-bg-opacity: 1;
        background-color: rgb(255 255 255 / var(--tw-bg-opacity));
        padding: 2rem;
      }

      .newnix_form .title {
        font-size: 1.5rem;
        line-height: 2rem;
        font-weight: 600;
      }

      .newnix_form .description {
        --tw-text-opacity: 1;
        color: rgb(75 85 99 / var(--tw-text-opacity));
      }

      .newnix_form .form {
        display: flex;
        flex-direction: column;
      }

      .newnix_form .form > :not([hidden]) ~ :not([hidden]) {
        --tw-space-y-reverse: 0;
        margin-top: calc(0.5rem * calc(1 - var(--tw-space-y-reverse)));
        margin-bottom: calc(0.5rem * var(--tw-space-y-reverse));
      }

      .newnix_form .group {
        display: flex;
        align-items: center;
      }

      .newnix_form .group > :not([hidden]) ~ :not([hidden]) {
        --tw-space-x-reverse: 0;
        margin-right: calc(0.5rem * var(--tw-space-x-reverse));
        margin-left: calc(0.5rem * calc(1 - var(--tw-space-x-reverse)));
      }

      .newnix_form .form .input {
        width: 100%;
        --tw-bg-opacity: 1;
        background-color: rgb(243 244 246 / var(--tw-bg-opacity));
        --tw-text-opacity: 1;
        color: rgb(14 17 30 / var(--tw-text-opacity));
      }

      .newnix_form .form .input,
      .newnix_form .form .button {
        border-radius: 0px;
        padding-left: 1rem;
        padding-right: 1rem;
        padding-top: 0.5rem;
        padding-bottom: 0.5rem;
      }

      .newnix_form .form .button {
        min-width: -webkit-fit-content;
        min-width: -moz-fit-content;
        min-width: fit-content;
        --tw-border-opacity: 1;
        border-color: rgb(107 114 128 / var(--tw-border-opacity));
        --tw-bg-opacity: 1;
        background-color: rgb(107 114 128 / var(--tw-bg-opacity));
        --tw-text-opacity: 1;
        color: rgb(255 255 255 / var(--tw-text-opacity));
      }

    </style>
  </head>
  <body>
    <%= @inner_content %>
  </body>
</html>
